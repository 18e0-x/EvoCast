# EvoCast Config Layout

`configs/` is organized by the subsystem each file controls:

- `providers/`: LLM provider and per-stage model settings.
- `policies/`: runtime policies for experiments, agent control, and quality gates.
- `registry/`: model registry overrides and aliases.
